import { CarouselIndicatorPipe } from './carousel-indicator.pipe';

describe('CarouselIndicatorPipe', () => {
  it('create an instance', () => {
    const pipe = new CarouselIndicatorPipe();
    expect(pipe).toBeTruthy();
  });
});
